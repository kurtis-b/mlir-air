// Copyright (C) 2026, Advanced Micro Devices, Inc.
// SPDX-License-Identifier: MIT
//
// Phase F study harness: the host-only test suite, behind the one entry point.
//
// This is doc 09 work item 5 -- "define how it is installed, discovered and
// invoked from CMake/lit -- a single .lit wrapper so `ninja check-...` remains
// the one entry point". lit discovers *.lit by glob, so this file needs no
// registration; the suite it runs discovers test_*.py by glob for the same
// reason.
//
// No NPU, no XRT, no compilation and no framework. The study tier's schema,
// results I/O, smoke gate, manifest, iron adapter and ladder shape override are
// all pure host logic, so they gate here rather than waiting for a device.
// pytest is not installed in the sandbox venv and installing it while a gate or
// a measurement run is live is the hazard doc 09 records, so these modules run
// as plain scripts -- written to be collectible by pytest unchanged if it ever
// lands, but depending on none of it. Same choice, and the same reasoning, as
// run_block_cache_tests.lit.
//
// THE COUNT IS PINNED ON PURPOSE. Discovery alone cannot catch a test that
// stops being *defined*: delete a test function and a globbing runner happily
// reports a smaller suite passing. Convention rule 11 exists because iron's
// pytest.ini collected none of its own study/test_*.py by directory, and that
// hid real bugs. So the module count and the test count are both matched
// exactly, and a suite that quietly shrinks fails here instead of passing.
//
// Update the numbers below when you add tests. That edit is the point, not
// friction to route around with a looser pattern.
//
// `[2026-08-12]` 231 -> 265, queue item 15: the measurement-condition block
// (doc 34 M4) in schema.py + manifest.py, and compare_roots' pmode guard. +12
// schema (the block is NOT a CSV table and did NOT bump SCHEMA_VERSION, so
// every recorded v2 CSV still reads), +10 manifest (the probe, against a stub
// xrt-smi on PATH -- this suite stays hermetic), +12 compare_roots (both
// branches of the guard, each asserted as a difference against the same drift).
//
// `[2026-08-12]` 299 -> 338, queue item 16: the TOOLCHAIN block. `compare_roots`
// has diffed a `toolchain` manifest block since it was written and `manifest.py`
// never wrote one, so that half of every root comparison compared NOTHING. +14
// schema (the block, its reader-only `absent`, and a pin that the KEY is the one
// compare_manifests diffs -- rename it and the diff goes quietly empty again),
// +9 manifest (the probe, against a scratch version.json -- this suite stays
// hermetic and never asserts THIS host's toolchain), +14 compare_roots (the
// guard FLAGS where the pmode guard REFUSES, so the load-bearing one asserts an
// identical drift produces an IDENTICAL FAIL set at matched and mismatched
// toolchains: this guard may add a warning and may withdraw nothing).
// SCHEMA_VERSION stays 2 and all 16 recorded v2 CSVs still read. +2 run_profile
// for the block reaching a REAL artifact: a walk records the toolchain it
// measured against, and `--gate-only` deliberately does NOT stamp today's
// versions onto a tree measured earlier.
//
// `[2026-08-12]` 338 -> 346, queue item 19: the device preflight. A
// `--compile-mode` parsed and never branched on dispatched off-queue while devq
// job 252 held the device lock, and the lock is advisory -- nothing outside the
// broker consults it. +8 run_profile: the three-valued `device_preflight`
// (free / held / indeterminate, refuse-known and flag-unknown, driven against a
// STUB broker so this suite still touches no device), a pin that no flag can
// skip it, and two audits that ARE the item -- that main() branches on the
// result before taking the output lock, and that no flag this module parses is
// left unread. The broker half is gated separately by
// agents/scripts/devq-selftest.sh TEST 7.
//
// `[2026-08-12]` 346 -> 357, the transcription/entry-point batch. No new module.
// +8 test_ladder_report: the module never exercised `ladder_report.load` or
// `main` -- it hand-built the POST-load row shape, so every check in it held
// whether or not `load` produced that shape at all. The eight run the real
// entry points over CSVs the shipped writer wrote, and one of them pins the
// in-memory fixture against what `load` actually attaches. Five injected
// defects in load/main are rejected by 0 of the 11 pre-existing tests.
// +1 test_run_ladder: `_spec()` is a hand-written stand-in for a real SPECS
// row (deliberately not imported -- opcheck_specs needs a toolchain this
// suite does not), now pinned to the catalogue by an ast walk of its source.
// +1 test_profiles: `expected_files()` was compared against a TYPED list of
// the four CSV names; it is compared against the files a stubbed
// `run_ladder.walk` actually writes, which is what the assertion claimed.
// +1 test_component_groups: the mode -> pattern-module map was typed, and it
// named `pattern/coarse/cells.py` while `coarse.py` sat beside it unread --
// undetectable, since BOTH open zero host buckets. The whole package is read
// now, with a control asserting exactly one mode opens any.
//
// `[2026-08-12]` 357 -> 429 and 19 -> 21 modules, queue item 25: the balance
// instrument (doc 47). +29 test_balance_ert: the ERT's argument requirement
// (a `dma_transfer` priced as a scalar is REFUSED -- doc 44's named defect),
// exact lookup on strides, the value/source pairing that stops an unpriced
// action reading as a free one, and repeat measurements folded into a spread
// rather than overwritten. +43 test_balance: the [step x port] demand matrix,
// the back-solve, the slope (an over-budget column is PRICED and still present,
// which is doc 44's correction of this project's own legality-predicate
// proposal), the bottleneck argmax, and iron's two instrument defects (doc 38
// §3.3) made structurally impossible. 16 injected defects across both modules
// are rejected -- including the two that matter most: making the budget a
// legality predicate again, and pricing a `dma_transfer` without its strides.
//
//
// `[2026-08-12]` 357 -> 393 and 19 -> 20 modules, queue item 26: the static
// legality predicate and the mapping-space census (study/mapping_space.py).
// +36 test_mapping_space, and the module count moves too because this is the
// first new module here since the count was pinned -- a suite that gained tests
// without gaining a module would be a test file edited, not a tier added, and
// the two numbers say which happened.
//
// Three of those 36 are the load-bearing ones and none of them is a unit test
// in the usual sense. Two REPRODUCE MEASUREMENTS with no compiler in the loop
// (R1's 7-of-16 per-column shim census and both memtile rows, doc 31b section
// 3.6; the herd inventory that refuses at nine herds, section 3.5), one
// brute-forces whole sub-spaces against the factorisation the census counts
// with. The census itself is NOT here -- it is run_mapping_space_tests.lit,
// deliberately a separate arm, because "the predicate is right" and "here is
// what it measures" are two claims and one recipe asserting both would let
// either pass on the other's evidence.
//
//
// `[2026-08-12]` The two items above landed in the same session and their
// counts were written against the same 357 baseline, so neither literal is the
// truth after both merged. This number was MEASURED on the merged tree rather
// than arrived at by adding the two deltas -- 72 + 36 happens to be right here,
// but a count reconciled by arithmetic is a count nobody ran.
//
//
// `[2026-08-12]` 357 -> 396 and 19 -> 20 modules, RESUME (doc 10 work item 8).
// A `ladder` walk is ~45 min cold on a laptop that suspends on a lid close, and
// a reboot at minute 40 used to cost all of it. +24 the new test_resume module,
// +4 run_ladder (a carried rung must reach no child process and be written BYTE
// FOR BYTE, a skip must beat a reuse, and the per-rung hook must see every
// rung), +9 schema (the walk block, its reader-only `absent`, and the pin that
// matters most: SCHEMA_VERSION STAYS 2 and there is no `session_id` COLUMN --
// attribution rides a manifest block precisely because a column would take
// every surviving v2 root out of every reader, item 15's decision unchanged),
// +2 run_profile for a hole the conditions block left when it landed before its
// caller: `gate` never passed `conditions=`, so every profile manifest recorded
// `npu_power_mode: unknown` on a run that had just REFUSED to start off Turbo.
//
// EVERY NEW CLAUSE IS PAIRED WITH THE BROKEN INPUT THAT MAKES IT FIRE, because
// a resume's natural implementation is bookkeeping and bookkeeping agrees with
// itself whatever the walk did -- which is the shape of both defects G0 closed.
// The load-bearing one is a walker that IGNORES `reuse` and re-measures: the
// plan, the ledger and the run report would all still say "reused", and only
// re-hashing the row afterwards catches it.
//
// `[2026-08-12]` 396 -> 403, THE COVERAGE SWEEP -- and the count moved by seven
// because the work was seven tests, not a sweep. `run_mode._shape_for` varied
// only `seq_len`, which is why five of six declared families read as
// unreachable; the registry coverage those two width families were said to need
// had been in the tree since 2026-08-07 and nothing had read the file. +4
// run_ladder (width override, no spec mutation on the second axis either, and a
// decoder REFUSED rather than measured as an encoder under a causal name), +3
// profiles net (the reachability check now re-derives from the registry JSON
// instead of from opcheck_specs' widths -- same idiom, right file; the fused
// bound is derived from the 2^20 plane cap so it MOVES with the width, 256..2048
// at emb 512; and a retarget re-derives every expected count with no number
// edited).
//
// `[2026-08-12]` 403 -> 409, doc 10's WORK ITEM 5 -- "write the prerequisites
// and recovery sections of the example README", taken as a CHECK. The item's
// requirement is its last four words, "rather than mid-suite"; a README
// paragraph cannot fail, and this project's own record is of prose rules that
// were true when written and silently stopped being (README trap 0 lived in
// prose until the conditions block moved it into the artifact). Doc 10's
// six-tool table is separately obsolete -- five are already recorded as dropped
// with a measurement each, and the sixth is only READ here -- so the
// prerequisite that was actually unguarded is not a binary: it is the two
// Python modules a bare devq shell lacks, `pyxrt` and `ml_dtypes`, BOTH of
// which fail late. +6 run_profile: each of the three clauses driven by the
// broken input that makes it fire, a control that a good shell produces no
// problems at all, and an audit that the check runs before the lock AND before
// the device is read.
//
//
// `[2026-08-12]` Third reconciliation of the evening. Items 25, 26 and Phase
// G's G1 all moved this literal against the same 357 baseline, so none of the
// three numbers is true after all three merged. Every note above is kept and
// the number below was MEASURED on the merged tree. A count reconciled by
// adding deltas is a count nobody ran.
//
// `[2026-08-12]` 517 -> 519, no module added: the `baseline_1024` registry gap
// that G1 left open was not a gap, and the test that guarded it could not have
// said so. `KNOWN_REGISTRY_GAPS` held one entry -- `2048x1024x3072` has no
// `drain` row, therefore `offload`/`runlist` fail at seq 2048 -- whose registry
// half was true and whose consequence was false: neither mode resolves a `3h`
// shape at all, and the only consumer of one pins `fused-cast`, which is
// present. The old clause asserted only "the method is still missing", which is
// true of every method nobody asks for, so it would have passed forever.
//
// +2 test_profiles, and both are about the difference between a MISSING method
// and a DEMANDED one. `test_a_declared_registry_gap_must_be_one_some_mode
// _actually_demands` adds the second direction -- a gap must also name a
// (triple, method) that `PINNED_PROJECTION_METHODS` says a consumer pins -- and
// the deleted entry is its negative control, since no role at `N = 3h` pins
// `drain`. `test_the_pinned_methods_are_re_derived_from_the_modules_that_pin
// _them` keeps that new table honest by `ast`-reading the two files that carry
// the pins (`qkv_proj.SCRATCH_METHOD`, offload's `_chain_spec` re-resolution),
// because a table copied out of a source file agrees with the day it was copied.
// The pre-existing reachability test also stopped checking only that the TRIPLE
// is present and now checks that the PINNED METHOD is, which is the check that
// would have caught this class in the first place.
//
// RUN: python3 %S/study/run_host_tests.py | FileCheck %s
// CHECK: study host tests: 519/519 passed in 23 modules
